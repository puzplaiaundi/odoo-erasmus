Place your AcroForm PDF template here as `contrato_template.pdf`.

Required form field names:
- nombre_apellido
- direccion_completa

The module will load it from `gestion_erasmus/static/src/pdf/contrato_template.pdf` and fill fields using pdfrw.
