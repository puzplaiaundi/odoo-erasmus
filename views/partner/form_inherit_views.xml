<odoo>
  <data>
    <!-- Referencias postales eliminadas: se usa autocompletado dinámico con Nominatim -->
    
    <!-- Inherit res.partner form to add navigation to Persona Erasmus -->
    <record id="view_res_partner_form_inherit_erasmus" model="ir.ui.view">
      <field name="name">res.partner.form.inherit.erasmus</field>
      <field name="model">res.partner</field>
      <field name="inherit_id" ref="base.view_partner_form"/>
      <field name="arch" type="xml">
        <xpath expr="//div[@name='button_box']" position="inside">
          <button name="action_open_or_create_persona" type="object" class="oe_stat_button" icon="fa-user">
            <div class="o_stat_info">
              <span class="o_stat_text">Persona Erasmus</span>
            </div>
          </button>
        </xpath>
      </field>
    </record>
  </data>
</odoo>
